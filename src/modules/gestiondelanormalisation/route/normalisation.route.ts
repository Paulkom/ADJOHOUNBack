/*import * as express from 'express';
import { testNormalisation } from '../controller/normalisation.controller';

export  const normalisation_routes =  (router: express.Router) => {
  router.post('/api/normalisation', testNormalisation);
};*/